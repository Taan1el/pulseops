# Copy to .env and adjust if needed. npm run dev and npm start both load
# it automatically if it exists (node --env-file-if-exists=.env); an
# exported environment variable still takes priority over the file.

# Port the API listens on.
PORT=4000

# Path to the SQLite database file. Created automatically if missing.
DATABASE_PATH=./data/pulseops.db
