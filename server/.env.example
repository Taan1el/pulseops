# Copy to .env and adjust if needed. The server reads these from the
# process environment (see server/src/index.ts); this file is reference
# only; nothing loads it automatically.

# Port the API listens on.
PORT=4000

# Path to the SQLite database file. Created automatically if missing.
DATABASE_PATH=./data/pulseops.db
