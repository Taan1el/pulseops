# Loaded automatically by `vite build --mode pages` (see the build:pages
# script). Turns on the in-browser demo API so the GitHub Pages build works
# with no backend. Not used by the normal dev server or production build.
VITE_DEMO_MODE=true
