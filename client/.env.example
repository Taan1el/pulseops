# Copy to .env.local to override the Vite dev server's API proxy target
# (see client/vite.config.ts). An exported VITE_API_TARGET environment
# variable works too and takes priority over this file.

VITE_API_TARGET=http://localhost:4000
